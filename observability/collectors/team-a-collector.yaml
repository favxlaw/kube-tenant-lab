apiVersion: v1
kind: ServiceAccount
metadata:
  name: otel-collector
  namespace: platform-observability
  labels:
    team: platform
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: otel-collector
  labels:
    team: platform
rules:
  - apiGroups: [""]
    resources: ["nodes", "pods", "namespaces"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: otel-collector
  labels:
    team: platform
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: otel-collector
subjects:
  - kind: ServiceAccount
    name: otel-collector
    namespace: platform-observability
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: otel-collector-team-a-config
  namespace: platform-observability
  labels:
    team: platform
    tenant: team-a
data:
  config.yaml: |
    receivers:
      filelog:
        include:
          - /var/log/pods/team-a-demo_*/*/*.log
        start_at: beginning
        include_file_path: true
        include_file_name: false
        operators:
          - type: json_parser
            if: 'body matches "^\\{"'
          - type: move
            from: attributes["log.file.path"]
            to: resource["log.file.path"]
      otlp:
        protocols:
          grpc:
            endpoint: 0.0.0.0:4317
    processors:
      resource:
        attributes:
          - key: team
            value: team-a
            action: insert
          - key: namespace
            value: team-a-demo
            action: insert
          - key: platform
            value: kube-tenant-lab
            action: insert
      batch:
        timeout: 5s
        send_batch_size: 1000
    exporters:
      otlp:
        endpoint: otel-gateway.platform-observability.svc.cluster.local:4317
        tls:
          insecure: true
    service:
      pipelines:
        logs:
          receivers: [filelog]
          processors: [resource, batch]
          exporters: [otlp]
        traces:
          receivers: [otlp]
          processors: [resource, batch]
          exporters: [otlp]
        metrics:
          receivers: [otlp]
          processors: [resource, batch]
          exporters: [otlp]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: otel-collector-team-a
  namespace: platform-observability
  labels:
    app: otel-collector-team-a
    team: platform
    tenant: team-a
spec:
  replicas: 1
  selector:
    matchLabels:
      app: otel-collector-team-a
  template:
    metadata:
      labels:
        app: otel-collector-team-a
        team: platform
        tenant: team-a
      annotations: {}
    spec:
      serviceAccountName: otel-collector
      nodeSelector:
        role: team-a
      tolerations:
        - key: "team"
          operator: "Equal"
          value: "team-a"
          effect: "NoSchedule"
      containers:
        - name: otel-collector
          image: otel/opentelemetry-collector-contrib:0.96.0
          args:
            - "--config=/conf/config.yaml"
          volumeMounts:
            - name: config
              mountPath: /conf
            - name: varlogpods
              mountPath: /var/log/pods
              readOnly: true
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "200m"
      volumes:
        - name: config
          configMap:
            name: otel-collector-team-a-config
        - name: varlogpods
          hostPath:
            path: /var/log/pods
---
apiVersion: v1
kind: Service
metadata:
  name: otel-collector-team-a
  namespace: platform-observability
  labels:
    app: otel-collector-team-a
    team: platform
    tenant: team-a
spec:
  selector:
    app: otel-collector-team-a
  ports:
    - name: otlp-grpc
      port: 4317
      targetPort: 4317
      protocol: TCP
  type: ClusterIP
